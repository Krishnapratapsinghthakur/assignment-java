import java.util.Scanner;
class Test{
    public static void main(String args[]){
         Scanner sc=new Scanner(System.in);
    String s=sc.next();
    s="krishna";
    // System.out.println(s.charAt(0));
    System.out.println("\"kri\tshna\"");



//     \n: Newline - Moves the cursor to the beginning of the next line.
// \t: Tab - Inserts a horizontal tab space.
// \r: Carriage Return - Moves the cursor to the beginning of the current line (can overwrite previous text on the same line).
// \\: Backslash - Represents a literal backslash character.
// \': Single Quote - Represents a literal single quote character.
// \": Double Quote - Represents a literal double quote character.
// \b: Backspace - Moves the cursor back one character (can delete the character before it in some contexts).
// \f: Form Feed - Inserts a form feed character (often used for page breaks in printing). 
    }
   
}
